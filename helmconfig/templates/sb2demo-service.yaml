apiVersion: v1
kind: Service
metadata:
  name: quotes-service
spec:
  ports:
    - name: http
      port: {{.Values.service.port }}
      protocol: TCP
      targetPort: {{.Values.service.port}}
  selector:
    app: {{.Values.appName}}
  type: {{.Values.service.type}}
